This folder just contains some files that represent a milestone in this
compiler project (and the custom computer project as a whole).

small_victory.c, small_victory.s, small_victory, and the screenshot all show
that the compiler can now generate code that will actually run, even though
the code generator is far from complete. 
